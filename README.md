# Визуальный редактор формы

Веб-приложение для визуального редактирования и настройки форм с поддержкой различных стран и языков.

## 🚀 Быстрый старт (Docker)

### Локальный запуск

1. Убедитесь, что Docker и Docker Compose установлены на вашем компьютере
2. Перейдите в директорию проекта
3. Запустите контейнер:

```bash
docker-compose up -d --build
```

4. Откройте браузер и перейдите по адресу: `http://localhost:8085`

### Остановка контейнера

```bash
docker-compose down
```

### Просмотр логов

```bash
docker-compose logs -f
```

## 📦 Развертывание на сервере

### Вариант 1: С использованием docker-compose

1. Скопируйте все файлы проекта на сервер
2. Запустите:

```bash
docker-compose up -d --build
```

3. Приложение будет доступно на порту 8085

### Вариант 2: Ручная сборка образа

```bash
# Сборка образа
docker build -t visual-form-editor .

# Запуск контейнера
docker run -d \
  --name visual-form-editor \
  -p 8085:80 \
  --restart unless-stopped \
  visual-form-editor
```

## 🔧 Настройка порта

Порт по умолчанию: **8085**

Чтобы изменить порт, отредактируйте `docker-compose.yml`:

```yaml
ports:
  - "ВАЫШ_ПОРТ:80"  # Например: "9090:80"
```

## 📝 Особенности

- Визуальный редактор форм с drag-and-drop
- Поддержка множества стран и языков
- Автосохранение изменений в localStorage
- Анимация заполнения формы
- Адаптивный дизайн

## 🛠️ Технологии

- HTML5 / CSS3 / JavaScript
- Interact.js (для drag-and-drop)
- Nginx (веб-сервер)
- Docker (контейнеризация)

## 📊 Структура проекта

```
.
├── index.html          # Главная страница
├── style.css           # Стили приложения
├── script.js           # Логика приложения
├── data.js             # Данные по странам
├── Dockerfile          # Конфигурация Docker
├── docker-compose.yml  # Docker Compose конфигурация
├── nginx.conf          # Конфигурация Nginx
└── README.md           # Документация
```

## 🔒 Безопасность

- Скрыта версия Nginx
- Включено gzip-сжатие
- Настроено кэширование статических ресурсов
- Health check для мониторинга состояния

## 📞 Поддержка

При возникновении проблем проверьте:
- Доступность порта 8085 (не занят другим приложением)
- Логи контейнера: `docker logs visual-form-editor`
- Статус контейнера: `docker ps -a`
