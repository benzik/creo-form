# 🐳 Развертывание через Portainer

## 📋 Быстрая инструкция для Portainer

### Вариант 1: Через Git Repository (Рекомендуется)

1. **Откройте Portainer** и перейдите в **Stacks**
2. Нажмите **+ Add stack**
3. Введите имя стека: `visual-form-editor`
4. Выберите **Repository**
5. Заполните поля:
   - **Repository URL:** `https://github.com/benzik/creo-form`
   - **Repository reference:** `refs/heads/main`
   - **Compose path:** `docker-compose.yml`
6. Нажмите **Deploy the stack**

✅ **Готово!** Приложение развернется автоматически на порту **8085**

---

### Вариант 2: Через Web editor

1. **Откройте Portainer** и перейдите в **Stacks**
2. Нажмите **+ Add stack**
3. Введите имя стека: `visual-form-editor`
4. Выберите **Web editor**
5. Скопируйте и вставьте содержимое из секции ниже
6. Нажмите **Deploy the stack**

#### Содержимое для Web editor:

```yaml
name: visual-form-editor-project

services:
  form-editor:
    image: ghcr.io/benzik/creo-form:latest  # Или соберется локально
    build:
      context: https://github.com/benzik/creo-form.git
      dockerfile: Dockerfile
    container_name: visual-form-editor
    ports:
      - "8085:80"
    restart: unless-stopped
    networks:
      - form-editor-network
    healthcheck:
      test: ["CMD", "sh", "-c", "pgrep nginx"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  form-editor-network:
    driver: bridge
```

---

## 🔧 Настройка порта

Если порт **8085** занят, измените в настройках стека:

```yaml
ports:
  - "ВАЫШ_ПОРТ:80"  # Например: "9090:80"
```

---

## 🌐 Настройка Caddy (проксирование на поддомен)

### Шаг 1: Добавьте контейнер в сеть Caddy

В Portainer откройте стек и добавьте сеть Caddy:

1. **Stacks** → `visual-form-editor` → **Editor**
2. Раскомментируйте строки с `caddy_network`:

```yaml
networks:
  - form-editor-network
  - caddy_network  # ← Раскомментировать
```

И в секции networks внизу:

```yaml
networks:
  form-editor-network:
    driver: bridge
  caddy_network:  # ← Раскомментировать
    external: true
    name: caddy_caddy  # Проверьте имя: docker network ls
```

3. **Update the stack**

### Шаг 2: Узнайте имя сети Caddy

```bash
docker network ls | grep caddy
```

Обычно это `caddy_caddy` или `caddy_default`. Укажите правильное имя в `docker-compose.yml`

### Шаг 3: Настройте Caddyfile

**ВАЖНО:** Используйте имя контейнера и внутренний порт 80!

```caddy
forms.your-domain.com {
    reverse_proxy visual-form-editor:80
    #              ↑имя контейнера  ↑внутренний порт (НЕ 8085!)
    
    # Опционально: логи
    log {
        output file /var/log/caddy/forms.log
    }
    
    # Опционально: заголовки безопасности
    header {
        Strict-Transport-Security "max-age=31536000;"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
    }
    
    encode gzip
}
```

### Шаг 4: Примените конфигурацию

```bash
# Если Caddy в Docker
docker exec caddy caddy reload

# Если Caddy установлен на хосте
sudo systemctl reload caddy
```

### Шаг 5: Проверьте

```bash
# Проверьте доступность из контейнера Caddy
docker exec caddy wget -qO- http://visual-form-editor:80 | head

# Должен вывести HTML код страницы
```

✅ Готово! Приложение будет доступно по адресу `https://forms.your-domain.com`

---

## 🔍 Troubleshooting Caddy

### Ошибка "dial tcp: lookup visual-form-editor"

**Причина:** Контейнеры не в одной сети

**Решение:**
```bash
# Проверьте сети контейнера
docker inspect visual-form-editor | grep NetworkMode

# Добавьте контейнер в сеть Caddy вручную
docker network connect caddy_caddy visual-form-editor
```

### Ошибка "connection refused"

**Причина:** Неверный порт в Caddyfile

**Решение:** Используйте порт **80** (внутренний), а не 8085:
```caddy
reverse_proxy visual-form-editor:80  # ← Правильно
reverse_proxy localhost:8085         # ← Неправильно для Docker сети
```

### Как узнать правильное имя сети?

```bash
# Посмотрите все сети
docker network ls

# Посмотрите детали сети Caddy
docker network inspect caddy_caddy
```

---

## 📊 Проверка статуса в Portainer

1. Перейдите в **Stacks**
2. Найдите стек `visual-form-editor`
3. Убедитесь, что статус контейнера: **running (healthy)**
4. В разделе **Published ports** должен быть: `0.0.0.0:8085->80/tcp`

---

## 🔍 Просмотр логов в Portainer

1. Откройте **Containers**
2. Найдите контейнер `visual-form-editor`
3. Нажмите на него
4. Перейдите в раздел **Logs**

---

## 🔄 Обновление приложения

### Через Portainer:

1. Перейдите в **Stacks** → `visual-form-editor`
2. Нажмите **Pull and redeploy**
3. Подтвердите действие

### Вручную через SSH:

```bash
cd /opt/visual-form-editor
git pull
docker-compose down
docker-compose up -d --build
```

---

## ⚙️ Переменные окружения (если понадобятся)

В Portainer можно добавить переменные окружения в разделе **Environment variables**:

```env
# Пример (для будущих настроек)
TZ=Europe/Moscow
```

---

## 🆘 Troubleshooting

### Порт 8085 занят

В настройках стека измените порт:
```yaml
ports:
  - "9090:80"  # Используйте свободный порт
```

### Контейнер не запускается

1. Проверьте логи в Portainer
2. Убедитесь, что Docker daemon работает
3. Проверьте доступность порта: `sudo netstat -tulpn | grep 8085`

### Приложение недоступно

1. Проверьте статус контейнера: должен быть **running (healthy)**
2. Проверьте файрвол: `sudo ufw status`
3. При необходимости откройте порт: `sudo ufw allow 8085/tcp`

---

## 📝 Полезные ссылки

- **GitHub Repository:** https://github.com/benzik/creo-form
- **После развертывания:** http://YOUR_SERVER_IP:8085
- **С Caddy:** https://forms.your-domain.com

---

## ✅ Итоговый чеклист

- [ ] Portainer установлен и доступен
- [ ] Создан стек из GitHub репозитория
- [ ] Контейнер запущен и healthy
- [ ] Приложение доступно на порту 8085
- [ ] Caddy настроен на нужный поддомен
- [ ] SSL-сертификат получен автоматически (через Caddy)
- [ ] Всё работает! 🎉
